# Bodrix AI - دليل الإعداد الكامل

## 🎉 مرحباً بك!

تم بناء مشروعك بنجاح! هذا المشروع يحتوي على:

- ✅ نظام مصادقة آمن مع Google OAuth
- ✅ قاعدة بيانات MySQL متكاملة
- ✅ واجهة مستخدم جميلة وسهلة الاستخدام
- ✅ نظام تسجيل الدخول والخروج
- ✅ لوحة تحكم للمستخدمين

---

## 📋 المتطلبات

قبل البدء، تأكد من أن لديك:

1. **Node.js** (الإصدار 18 أو أحدث)
2. **npm** أو **pnpm**
3. **Google OAuth Credentials** (Client ID و Client Secret)
4. **Supabase Database URL** (اختياري - يمكنك استخدام أي قاعدة بيانات MySQL)

---

## 🚀 خطوات البدء

### الخطوة 1: تثبيت المتطلبات

```bash
cd bodrix-ai-final
pnpm install
# أو
npm install
```

### الخطوة 2: إعداد متغيرات البيئة

تم إضافة بيانات اعتماد Google تلقائياً. تأكد من أن المتغيرات التالية موجودة في ملف `.env`:

```
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
DATABASE_URL=your-database-url
```

### الخطوة 3: إعداد قاعدة البيانات

```bash
# إنشاء الجداول والهجرات
pnpm db:push
```

### الخطوة 4: تشغيل المشروع

```bash
# تشغيل خادم التطوير
pnpm dev
```

المشروع سيكون متاحاً على: `http://localhost:3000`

---

## 🔐 نظام المصادقة

### كيفية عمل تسجيل الدخول

1. المستخدم ينقر على "تسجيل الدخول عبر Google"
2. يتم إعادة التوجيه إلى صفحة Google للمصادقة
3. بعد المصادقة الناجحة، يتم إنشاء حساب في قاعدة البيانات
4. يتم حفظ جلسة المستخدم
5. يتم إعادة التوجيه إلى لوحة التحكم

### الملفات المهمة

- `server/_core/oauth.ts` - معالجة OAuth
- `server/routers.ts` - نقاط نهاية API
- `client/src/pages/Login.tsx` - صفحة تسجيل الدخول
- `client/src/pages/Dashboard.tsx` - لوحة التحكم

---

## 📁 هيكل المشروع

```
bodrix-ai-final/
├── client/                    # الواجهة الأمامية (React)
│   ├── src/
│   │   ├── pages/            # صفحات التطبيق
│   │   │   ├── Login.tsx      # صفحة تسجيل الدخول
│   │   │   ├── Dashboard.tsx  # لوحة التحكم
│   │   │   └── Home.tsx       # الصفحة الرئيسية
│   │   ├── components/        # مكونات React
│   │   ├── lib/               # مكتبات مساعدة
│   │   └── App.tsx            # التطبيق الرئيسي
│   └── public/                # ملفات ثابتة
├── server/                    # الخادم الخلفي (Express + tRPC)
│   ├── routers.ts            # نقاط نهاية API
│   ├── db.ts                 # استعلامات قاعدة البيانات
│   ├── storage.ts            # تخزين الملفات
│   └── _core/                # ملفات أساسية
├── drizzle/                  # قاعدة البيانات
│   ├── schema.ts             # تعريف الجداول
│   └── migrations/           # هجرات قاعدة البيانات
└── shared/                   # كود مشترك

```

---

## 🧪 تشغيل الاختبارات

```bash
# تشغيل جميع الاختبارات
pnpm test

# تشغيل اختبار معين
pnpm test -- server/auth.google.test.ts

# تشغيل الاختبارات مع المراقبة
pnpm test -- --watch
```

---

## 📝 ملاحظات مهمة

### 1. قاعدة البيانات

- المشروع يستخدم MySQL/TiDB
- تم تكوينه مع Drizzle ORM
- جميع الهجرات تتم تلقائياً عند تشغيل `pnpm db:push`

### 2. الأمان

- جميع بيانات المستخدم محمية
- كلمات المرور لا تُحفظ (نستخدم Google OAuth)
- جميع الاتصالات آمنة (HTTPS)

### 3. التطوير

- استخدم `pnpm dev` لتشغيل خادم التطوير
- استخدم `pnpm build` لبناء النسخة الإنتاجية
- استخدم `pnpm start` لتشغيل النسخة الإنتاجية

---

## 🐛 استكشاف الأخطاء

### المشكلة: "خطأ في المصادقة"

**الحل:**
1. تأكد من أن `GOOGLE_CLIENT_ID` و `GOOGLE_CLIENT_SECRET` صحيحة
2. تأكد من أن رابط Callback في Google Console صحيح
3. أعد تشغيل الخادم

### المشكلة: "خطأ في قاعدة البيانات"

**الحل:**
1. تأكد من أن `DATABASE_URL` صحيح
2. شغّل `pnpm db:push` لإنشاء الجداول
3. تحقق من اتصالك بالإنترنت

### المشكلة: "الصفحة لا تحمل"

**الحل:**
1. افتح أدوات المطور (F12)
2. تحقق من وجود أخطاء في Console
3. أعد تحميل الصفحة (Ctrl + Shift + R)

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشكلة:

1. تحقق من ملف `README.md` الرئيسي
2. ابحث عن الخطأ في الإنترنت
3. تحقق من أن جميع المتطلبات مثبتة بشكل صحيح

---

## 🎯 الخطوات التالية

بعد التأكد من أن المشروع يعمل:

1. **إضافة ميزات جديدة**: عدّل `server/routers.ts` لإضافة نقاط نهاية جديدة
2. **تخصيص التصميم**: عدّل `client/src/index.css` لتغيير الألوان والخطوط
3. **إضافة جداول جديدة**: عدّل `drizzle/schema.ts` ثم شغّل `pnpm db:push`
4. **نشر المشروع**: استخدم Vercel أو Railway أو أي منصة أخرى

---

## 📚 مراجع مفيدة

- [tRPC Documentation](https://trpc.io/)
- [Drizzle ORM](https://orm.drizzle.team/)
- [React Documentation](https://react.dev/)
- [Express.js](https://expressjs.com/)
- [Google OAuth](https://developers.google.com/identity/protocols/oauth2)

---

## ✨ تم بناء هذا المشروع بعناية فائقة!

استمتع بتطوير تطبيقك! 🚀
